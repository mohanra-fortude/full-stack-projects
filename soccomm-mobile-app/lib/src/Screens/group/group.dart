class group {
  group(this.name, this.type, this.createruserId);

  final String name;
  final String type;
  final String createruserId;

  getName() => this.name;

  getType() => this.type;

  getId() => this.createruserId;
}
